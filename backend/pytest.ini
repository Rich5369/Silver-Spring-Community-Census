[pytest]
# `pythonpath = .` lets tests import `app.*` without an editable install or any
# sys.path juggling in conftest.py. Requires pytest >= 7.0.
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts = -q
