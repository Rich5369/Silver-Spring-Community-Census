"""Operational command-line entry points.

Intended for one-off and scheduled tasks run outside the request cycle -
principally data ingestion - invoked as ``python -m scripts.<name>`` from the
``backend/`` directory. Empty until ingestion is implemented.
"""
